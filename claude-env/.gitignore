# OS files
.DS_Store
Thumbs.db
Desktop.ini

# MCP server configs (project-level, may contain tokens)
# Use mcp.template.json (tracked) as the token-free starting point
.mcp.json

# Battle memory files (tracked — cross-session team wisdom)
# .battle_memory.yaml → agent memory, ~30 lines
# .battle_archive.md → human archive, full session records

# Battle state file (temporary — deleted after each session)
.battle_state.yaml

# Squad runtime state (generated per-project, not tracked)
plugins/squad/.claude/
.worktrees/

# Serena
.serena

# OMC
.omc
